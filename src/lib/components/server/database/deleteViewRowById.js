
import deleteViewRowiD from '$lib/components/database/deleteViewRecord';

/**
 * @param { any } id
 */

export default async function deleteViewsRowByiD(id) {
    return await deleteViewRowiD(id);
}
